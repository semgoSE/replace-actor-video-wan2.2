# Минимальные зависимости для оркестрации пайплайна (run_preprocessing, run_generation, pipeline).
# Генерация выполняется в репозитории Wan2.2 — установите его зависимости отдельно.

numpy>=1.24.0
Pillow>=9.0.0

# Опционально для загрузки видео в preprocess.py:
# imageio>=2.28.0
# imageio-ffmpeg>=0.4.8
# или opencv-python-headless>=4.8.0
